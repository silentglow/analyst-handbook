# 内容新增与维护指南

这份指南解决一个核心问题：**以后有新知识点时，应该放在哪里，怎样整理，才能让站点越长越清晰，而不是越长越乱。**

## 一、先判断内容承担什么任务

新内容进入系统前，先回答三个问题：

1. 它是在建立一项长期稳定的核心能力吗？
2. 它需要学习者在具体业务情境中做判断吗？
3. 它是长期有效，还是具有明显时效性？

据此选择容器：

| 内容容器 | 适合的内容 | 不适合的内容 |
|---|---|---|
| 核心课程 | 会改变学习主线、能力顺序或必备框架的内容 | 单个行业新闻、零散技巧、重复案例 |
| 案例故事 | 有角色、目标、数据、冲突和判断节点的业务问题 | 纯概念解释、没有选择空间的案例 |
| 知识卡 | 定义、公式、方法边界、易混概念、延伸阅读 | 需要完整业务推进的长案例 |
| 小练习 | 选择题、追问题、口述题、评分表、短数据题 | 需要十几分钟阅读背景的材料 |
| 更新内容 | 平台变化、行业数据、工具更新、招聘趋势 | 长期基础框架 |
| 收件箱 | 尚未判断价值和归属的原始材料 | 已发布内容的永久存放地 |

### 判断流程

```text
新内容
├─ 是否改变核心学习主线？
│  └─ 是 → 更新现有课程；只有确有新能力阶段时才新增章节
├─ 是否需要在真实情境中做选择？
│  └─ 是 → 新增案例故事
├─ 是否是可复用方法或概念？
│  └─ 是 → 知识卡
├─ 是否适合反复短练？
│  └─ 是 → 小练习
├─ 是否强时效？
│  └─ 是 → 更新内容
└─ 暂不确定 → 收件箱，等待整理
```

## 二、核心课程如何维护

核心课程应当保持相对稳定。优先更新已有章节，而不是不断增加章节编号。

### 只有同时满足以下条件，才考虑新增课程章节

- 它是目标岗位的高频核心能力；
- 无法自然合并进现有 24 章；
- 它有明确的先修关系和学习结果；
- 删除它会让完整能力链出现缺口；
- 能提供框架、示范、练习与面试表达，而不只是概念说明。

### 课程元数据

课程在 `chapters.json` 中维护。每章至少包含：

| 字段 | 作用 |
|---|---|
| `slug` | 稳定页面标识；发布后不要随意修改 |
| `num` | 展示章节编号 |
| `title` / `short_title` | 完整标题与目录短标题 |
| `module` | 所属模块 ID，对应 `modules.json` |
| `content_type` | `guide`、`case`、`concept`、`interview` 或 `career` |
| `duration` | 预计学习分钟数 |
| `description` | 页面摘要与搜索描述 |
| `objectives` | 2—3 个可验证学习目标 |
| `framework` | 面试速览中的核心步骤 |
| `interview_answer` | 可口述的 90 秒回答核心 |
| `pitfalls` | 常见失分点 |

正文源文件位于 `content/<slug>.html`。正文只写课程内容，不重复写页面标题、导航、学习目标或复习卡；这些由构建器统一生成。

## 三、案例故事如何新增

故事负责“让学习者做判断”，而不是把课程内容换一种排版再讲一遍。

### 合格故事应包含

1. **角色**：学习者现在是谁；
2. **目标**：在什么时间和限制下解决什么问题；
3. **证据**：逐步出现的数据、事件和访谈；
4. **选择**：至少一个真正会改变分析方向的判断点；
5. **反馈**：解释为什么某个选择更有效；
6. **复盘**：把故事里的判断迁移回课程框架。

### 新增步骤

1. 把原始 HTML 放入 `story-src/`，使用 `story-数字-英文-slug.html` 命名。
2. 在 `stories.json` 新增一条记录。
3. 运行 `python3 build.py`。
4. 检查故事顶部公共导航、上一篇/下一篇、底部判断复盘和移动端显示。

### 故事元数据

| 字段 | 作用 |
|---|---|
| `id` | 稳定 ID，例如 `story-26` |
| `num` | 展示编号 |
| `slug` | 英文 URL 标识 |
| `title` / `subtitle` | 标题与一句话冲突 |
| `category` | 故事库筛选分类 |
| `tags` | 2—4 个检索标签 |
| `difficulty` | 入门、进阶或挑战 |
| `duration` | 预计阅读分钟数 |
| `source` | `story-src/` 中的源文件名 |
| `output` | `stories/` 中的输出文件名 |
| `featured` | 是否在首页推荐 |
| `status` | 内容状态 |

## 四、内容状态与整理节奏

建议统一使用以下状态：

- `idea`：只有想法；
- `draft`：已开始写，但不可发布；
- `review`：等待内容、数据和体验检查；
- `published`：正式进入构建结果；
- `archived`：保留来源，但不再推荐或发布。

目前故事已使用 `status`。后续如果知识卡和练习数量增加，也应建立对应 JSON 清单，而不是只依赖文件名。

### 每次新增时

- 先放对容器；
- 写清学习目标和与现有内容的关系；
- 删除重复解释；
- 构建并检查。

### 每月或每批次整理

- 清空 `content/inbox/` 中已判断的材料；
- 合并重复知识点；
- 检查更新内容是否过期；
- 检查新增内容是否与现有内容重复，入口是否清晰；
- 根据真实学习反馈调整顺序，不根据“页面数量”判断成果。

## 五、统一质量标准

发布前至少回答“是”：

- 学习者进入页面后，30 秒内知道自己要完成什么吗？
- 内容是否有明确顺序，而不是信息堆叠？
- 案例是否让学习者先判断，而不是一路被动阅读？
- 结论是否有证据，且区分了线索、假设和根因？
- 是否能说出可执行动作、风险和监控？
- 面试速览内容是否可以在 2 分钟内回顾？
- 手机阅读是否不需要横向滚动？
- 页面是否能回到所属目录，并自然进入上一篇或下一篇？

## 六、视觉内容的使用标准

网页不能只是把连续文字放进不同卡片。每个较长章节至少应判断一次，是否需要视觉内容承担下列任务之一：

- **业务现场图**：还原角色、时间压力、资源容量与操作环境；
- **决策路径图**：展示问题可以走向分析、规则、实验、预测或退回定义；
- **数据机制图**：解释数据如何生成、在哪里被截断、泄漏或产生偏差；
- **失败复盘图**：展示事故信号、失败层级、止损与恢复流程。

视觉不是装饰。不能用会议室、键盘、人物握手等无关图片填空，也不要把正文原样复制进一张图。每张图必须具备：

1. 图前的观察问题，告诉学习者先看什么；
2. 描述核心结论的 `alt` 文本；
3. 图后的阅读重点或图注；
4. 本地可用的素材路径，避免依赖外部图片服务；
5. 桌面和手机均可阅读，不产生横向滚动。

本地教学图统一放入 `assets/visuals/`。课程源文件和故事源文件均使用 `../assets/visuals/文件名` 引用。优先使用响应式 SVG；一张图只表达一个核心机制，细节解释留在正文和图注中。 如果希望视觉内容同时出现在首页课程入口，可在 `chapters.json` 或 `stories.json` 中增加 `visual` 与 `visual_alt`；前者填写本地素材路径，后者写清这张图传达的核心结论。

## 七、构建原则

- `story-src/` 和 `content/` 是内容源，尽量保持可读、可迁移。
- `articles/`、`stories/`、`index.html`、`home.html` 是构建产物。
- 公共导航、双模式和故事复盘由 `build.py` 注入。
- 不直接修改构建产物；所有长期修改都应回到源文件或构建器。

## 八、机器学习内容的特殊准入规则

机器学习内容不能从算法名称开始。每个新主题必须先填写“业务建模定义卡”，并同时回答：

1. 谁在什么时点做决策；
2. 一行数据代表什么对象；
3. 结果在多长时间后能够稳定观察；
4. 预测时真正可以获得哪些字段；
5. 收到预测后可以采取什么具体动作；
6. 处理容量、错误成本和保护边界是什么；
7. 规则、人工、诊断分析或随机实验是否已经足够。

如果对象、时点、结果或动作中任一项无法明确，内容应停留在需求澄清阶段，不进入算法教学。一次合理的“拒绝建模”可以作为正确结论。

### 每个主题必须包含多种路径

合格的机器学习课程不能只有一个成功模型。至少应同时出现：

- 一个简单规则或朴素预测基线；
- 一个模型方案及其适用条件；
- 一个不应使用预测、应该改做分析或实验的对照问题；
- 一个离线成功但业务失败的案例；
- 一项叫停、降级或人工兜底方案。

### 失败案例的写作标准

失败案例不能只写“数据质量不好”或“模型不准确”，必须说明：

| 字段 | 要回答的问题 |
|---|---|
| 表面成绩 | 为什么团队一开始认为它成功 |
| 真实损失 | 上线后哪项业务结果恶化或没有改善 |
| 失败层级 | 目标、数据、验证、行动还是环境出了问题 |
| 可观察信号 | 当时有哪些迹象本可提前发现 |
| 止损动作 | 发现问题后怎样限制继续损失 |
| 修复方式 | 重新定义、重建数据、改变验证还是重做流程 |
| 沉淀机制 | 怎样把失败转成监控、保护规则或准入检查 |

可直接复制 `templates/ml-business-definition.md` 和 `templates/ml-failure-case.md` 开始整理。
